- name: dmo on filters
  hosts: local
  connection: local
  vars:
    topics: linux, shell, ansible
  tasks:
  - name: greeting
    ansible.builtin.debug:
      msg: "Hi mani {{ greeting | default ('Good Morning') }}"

  - name: string to list
    ansible.builtin.debug:
      msg: "{{ topics | split(',') }}"