- name: shell vs command module
  hosts: local
  connection: local
  tasks:
  - name: command & shell module test
    ansible.builtin.command: cat /etc/passwd | grep root
    register: user_output

  - name: print output 
    ansible.builtin.debug:
      msg: "{{ user_output }}"